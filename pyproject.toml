[project]
name = "rag_against_the_machine"
version = "0.1.0"
description = "RAG against the machine"
readme = "README.md"
requires-python = ">=3.10.14"
dependencies = [
    "accelerate>=1.14.0",
    "chonkie[code]>=1.7.0",
    "fire>=0.7.1",
    "pydantic>=2.13.4",
    "rank-bm25>=0.2.2",
    "torch>=2.13.0",
    "tqdm>=4.70.0",
    "transformers>=5.16.1",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=2.3.1",
]
